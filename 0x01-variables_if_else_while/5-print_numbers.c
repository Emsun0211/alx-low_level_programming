#include <stdio.h>

/**
 * main - this code prints the digit number from 0 to 10
 * with a new line
 * Return: always (0) success
 */

int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
