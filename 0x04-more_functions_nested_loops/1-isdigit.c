#include "main.h"

/**
 * _isdigit - this programme check for digit
 * @c:  is an input integer
 * Return: returns 1 if it is an integer and returns 0 otherwise
 */

int _isdigit(int c)
{

	if (c >= 0 && c <= 9)

	return (1);

	else

	return (0);
}
